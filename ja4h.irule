###############################################################################################
#  iRule to calculate JA4H "HTTP Request"
#  See JA4H spec on GitHub for more details
#  https://github.com/FoxIO-LLC/ja4/blob/main/technical_details/JA4H.md
#
#  Copyright (c) 2024, FoxIO, LLC.
#  All rights reserved.
#  Licensed under FoxIO License 1.1
#  For full license text and more details, see the repo root https://github.com/FoxIO-LLC/ja4
#
###############################################################################################

when RULE_INIT {
    # Maximum Accept-Language bytes we are willing to inspect. Anything longer
    # is truncated before the regex to prevent CPU amplification.
    set static::ja4h_max_al 64
}

when CLIENT_ACCEPTED {
    # Use these variables to define the -r (raw) and -o (original) switches defined in the JA4 spec
    # 0 = false/disabled (default) and 1 = true/enabled
    set ja4h_raw 1
    set ja4h_original 0
}

when HTTP_REQUEST priority 10 {
    # Strip any client-supplied X-JA4H* headers so a client cannot spoof the
    # fingerprint that the backend sees.
    foreach jh [HTTP::header names] {
        if { [string tolower ${jh}] starts_with "x-ja4" } {
            HTTP::header remove ${jh}
        }
    }

    # Collect JA4H "a" values
    set me [string range [string tolower [HTTP::method]] 0 1]
    set v  [string map {"." ""} [HTTP::version]]
    set c  "n"
    set r  "n"
    if { [HTTP::header exists "cookie"] } {
        set c "c"
    }
    if { [HTTP::header exists "referer"] } {
        set r "r"
    }

    set lang "0000"
    set alval [HTTP::header value "accept-language"]
    if { ${alval} ne "" } {
        # Cap length before any regex/string work (CPU amplification guard).
        if { [string length ${alval}] > $static::ja4h_max_al } {
            set alval [string range ${alval} 0 [expr {$static::ja4h_max_al - 1}]]
        }
        # Reference JA4H: strip "-", map ";"->",", lowercase, take the first
        # comma-delimited token, then its first 4 characters (the "0000" seed
        # supplies right-padding). No hex-encoding: digits and other characters
        # are kept verbatim per the reference implementation.
        set alval [string tolower [string map {"-" "" ";" ","} ${alval}]]
        set alval [string range [lindex [split ${alval} ","] 0] 0 3]
        if { [string length ${alval}] > 0 } {
            set lang [string replace $lang 0 [expr {[string length ${alval}] - 1}] ${alval}]
        }
    }

    # Collect JA4H "b" values
    set hc 0
    set hstr ""
    foreach hname [HTTP::header names] {
        set hl [string tolower ${hname}]
        # Exclude HTTP/2 pseudo-headers (":authority", ":method", ...), Cookie
        # (prefix match, e.g. "Cookie2") and Referer from the header list/count,
        # matching the reference - and regardless of the original-order flag
        # (the reference never puts Cookie/Referer in the header list). Client-
        # supplied X-JA4* headers were already stripped above.
        if { ([string index ${hname} 0] eq ":") \
             || [string match "cookie*" ${hl}] || (${hl} eq "referer") } {
            continue
        }
        incr hc
        append hstr "${hname},"
    }

    if { $hc > 99 } { set hc 99 }
    set hc [format "%02d" $hc]
    set hstr [string trimright ${hstr} ","]
    binary scan [sha256 ${hstr}] H* hhash
    set trunc_hhash [string range $hhash 0 11]

    # Collect JA4H "c" and "d" values
    set cstr   ""
    set clist  [list]
    set ckvstr ""
    foreach cname [HTTP::cookie names] {
        lappend clist ${cname}
    }
    if { ${ja4h_original} == 0 } {
        set clist [lsort ${clist}]
    }
    foreach ck ${clist} {
        append cstr   "${ck},"
        append ckvstr "${ck}=[HTTP::cookie value ${ck}],"
    }

    set cstr   [string trimright ${cstr}   ","]
    set ckvstr [string trimright ${ckvstr} ","]
    if { $c eq "c" } {
        binary scan [sha256 ${cstr}]   H* chash
        binary scan [sha256 ${ckvstr}] H* ckvhash
        set trunc_chash   [string range $chash   0 11]
        set trunc_ckvhash [string range $ckvhash 0 11]
    } else {
        set trunc_chash   "000000000000"
        set trunc_ckvhash "000000000000"
    }

    # Generate the JA4H fingerprint string and store it in a connection variable.
    # This rule does NOT insert the X-JA4H header itself - header injection is
    # handled by the separate ja4_http_header_injector iRule.
    set ja4h_fp "${me}${v}${c}${r}${hc}${lang}_${trunc_hhash}_${trunc_chash}_${trunc_ckvhash}"

    # If enabled, also compute the JA4H_r raw fingerprint. Note: we deliberately do
    # NOT include raw cookie values (ckvstr); cookie values frequently contain
    # session tokens. The cookie-value hash above already covers that dimension.
    if { ${ja4h_raw} } {
        set ja4hr_fp "${me}${v}${c}${r}${hc}${lang}_${hstr}_${cstr}"
    }
}
